# 英语学习平台技术实现方案

## 1. 项目概述

本项目是一个基于 Web 的英语学习平台，主要面向桌面端浏览器，同时兼容适配移动端。平台核心功能包括：
- 抓取公共英语文章作为学习素材
- 提供文章的音标和标准发音
- 支持用户在线录音
- 提供用户发音与标准发音的对比及打分（口语测评）
- 提供文章的语法分析与讲解

## 2. 技术栈架构

项目采用全栈框架 Next.js，利用其前后端同构的特性，实现前端交互展示与后端的轻量级业务逻辑。

### 2.1 前端技术栈
- **框架**：React 19 + Next.js 16 (App Router)
- **开发语言**：TypeScript
- **样式方案**：Tailwind CSS v4 (响应式设计，优先桌面端，适配移动端)
- **状态管理**：React Context / Zustand (视后续复杂度而定)
- **音视频处理**：HTML5 Web Audio API / MediaRecorder API
- **组件库**：可选用 Radix UI 或 shadcn/ui 提升开发效率

### 2.2 后端及服务端技术栈 (Next.js API Routes / Server Actions)
- **运行环境**：Node.js
- **网页抓取**：`cheerio` (静态页面) 或 `@sparticuz/chromium` + `puppeteer-core` (动态渲染页面)
- **API 聚合层**：Next.js 服务端作为 BFF (Backend For Frontend) 层，负责调用第三方 API 并整合数据返回给前端，避免客户端直接暴露第三方 API Key。

### 2.3 第三方服务选用
根据需求，项目中复杂的 AI 和语音处理功能将直接集成成熟的第三方云服务：
- **发音测评（口语打分）**：
  - **推荐方案**：微软 Azure Speech Service (Pronunciation Assessment) 或 科大讯飞语音评测 API。
  - **优势**：提供多维度的评分（准确度、流利度、完整度、重音等），支持单词和句子级别的诊断。
- **语法分析与音标生成**：
  - **推荐方案**：大语言模型 API (如 OpenAI GPT-4o / DeepSeek API / 智谱等)。
  - **优势**：Prompt 定制化强，可以一次性从原文中提取出详细的语法点、长难句分析和精确的 IPA 音标。
- **标准发音 (TTS)**：
  - **推荐方案**：Azure Text-to-Speech / OpenAI TTS / ElevenLabs。
  - **优势**：发音自然，支持多种口音和语速调整。

---

## 3. 核心功能实现方案

### 3.1 英语文章数据抓取
采用 Next.js 服务端进行轻量级抓取，抓取任务可以通过定时任务 (CRON job 结合 Vercel Cron) 或用户触发。

**实现流程**：
1. **目标确立**：选择允许抓取且内容质量高的公共英语网站 (如 VOA Learning English, BBC Learning English 等，需注意版权及 robots.txt)。
2. **抓取实现**：
   - 在 `app/api/scrape/route.ts` 或 Server Actions 中编写逻辑。
   - 使用 `fetch` 获取网页 HTML。
   - 使用 `cheerio` 解析 DOM 树，提取标题、正文段落、发布时间等元数据。
3. **数据预处理**：将提取到的纯文本内容进行分段存储（支持按句子或段落进行后续的学习交互）。

### 3.2 语法分析与音标获取
利用 LLM 提供强大的文本分析能力。

**实现流程**：
1. 服务端获取抓取到的文章文本后，构建 Prompt 提交给 LLM API。
   *Prompt 示例*: "请分析以下英语文章段落：1. 标出所有单词的 IPA 音标。 2. 分析其中的长难句及核心语法（如时态、从句等）。"
2. 将 LLM 返回的结构化数据（JSON 格式）解析并存储/返回给前端。
3. 前端在阅读界面提供交互：用户悬浮或点击某个单词/句子时，展示其音标和语法解析卡片。

### 3.3 标准发音与用户录音
**标准发音**：
- 前端通过 `<audio>` 标签或 Web Audio API 播放通过 TTS 服务生成的音频流。
- 可以对文本进行句子级的切分，用户点击某一句话即可发音。

**用户录音 (浏览器端)**：
- 使用 `navigator.mediaDevices.getUserMedia({ audio: true })` 获取麦克风权限。
- 使用 `MediaRecorder` API 将用户音频录制为 `webm` 或 `wav` 格式。
- **响应式处理**：在桌面端展示详细的录音波形图，在移动端适配为简洁的底部悬浮录音按钮，确保交互区域足够大。

### 3.4 发音测评与对比打分
录音完成后，将音频上传至服务端，服务端转发给第三方语音评测 API 进行打分。

**实现流程**：
1. 前端获取录音文件 (Blob对象)。
2. 前端通过 `FormData` 将音频和对应的参考文本发送至 Next.js API 路由 (`/api/assessment`)。
3. Next.js 服务端携带 API Key 请求微软 Azure Speech 或科大讯飞的测评接口。
4. 服务端接收评测结果，包括：综合得分、发音准确度、错读/漏读/多读的单词标记。
5. 前端拿到结果后，通过颜色高亮在原文上标注用户发音的问题（例如：绿色表示正确，红色表示发音错误），并给出雷达图或分数面板。

---

## 4. 数据库与存储 (可选/扩展)
考虑到初期是轻量级应用，如果不涉及大量的用户社交，可以采用：
- **数据库**：Vercel Postgres 或 Supabase（用于存储抓取到的文章结构化数据、用户的学习记录和历史得分）。
- **对象存储**：AWS S3 或 Vercel Blob（用于缓存 TTS 生成的标准音频，降低每次调用的成本）。

## 5. 安全与性能考量
1. **API 密钥安全**：所有第三方服务（LLM, TTS, 发音评测）的 API Key 必须存放于 `.env.local` 并在服务端调用，绝对不能暴露在前端代码中。
2. **服务端性能**：SSR (Server-Side Rendering) 可用于快速首屏加载文章列表。抓取和 LLM 分析操作较耗时，应使用异步处理或建立服务端缓存（如 Redis），避免每次用户访问都实时抓取和调用 LLM。
3. **移动端适配**：通过 Tailwind CSS 的响应式前缀 (`md:`, `lg:`) 进行布局切换，特别注意移动端录音权限的获取提示和交互体验。
